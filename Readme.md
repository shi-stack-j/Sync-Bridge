# CypherLink Configuration

This repository hosts the public configuration used by the CypherLink Sender application.

The Android application periodically downloads `config.json` to obtain the latest backend configuration.

Only non-sensitive configuration values are stored here.

No passwords, authentication tokens, API secrets or private information should ever be committed to this repository.